// const express = require("express");
// const router = express.Router();
// const uploadCtrl = require("../../middleware/upload");
// const upload = require("../../middleware/upload");
// const auth = require("../../middleware/authMiddleware");

// router.use(auth);

// router.post("/customer-image", upload.single("image"), uploadCtrl.uploadCustomerImage);

// router.delete("/customer-image", uploadCtrl.deleteCustomerImage);

// module.exports = router;